In the context of software codebases, clasify the next comment sentence as 'summary', 'ownership', 'expand', 'usage', 'pointer', 'deprecation' or 'rational'. It is possible to assign more than one class to a sentence. Answer only with the selected classes separated by semicolon (,) without quotation marks.

COMMENT SENTENCE:
{text}
